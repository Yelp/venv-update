#!/not/executable/bash
export TOX_TESTENV_PASSENV="CI $(env | grep -Po '^(CIRCLE|TRAVIS|COVERALLS)[^=]*')"

if [[ "${TOXENV:-}" = "python" && "${TRAVIS_PYTHON_VERSION:-}" ]]; then
    export PYTHON="$(sed 's/^[0-9]/python&/' <<< "$TRAVIS_PYTHON_VERSION")"
fi
